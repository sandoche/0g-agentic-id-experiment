# Implementation decisions and review

Recorded during the approved native execution workflow. Private policy and credentials are excluded.

- Ruling: Continue natively after the user asked for autonomous overnight work and authorized runs; do not wait on another process choice. Cost if wrong: the user may prefer a different review cadence. One independent final review remains required.
- Ruling: Use an isolated worktree on codex/agentic-portfolio and push each green milestone. Leave the user's populated .env in the original checkout; do not inspect private keys.
- Ruling: Use the bundled Node 24.19.0 locally, maintaining a Node >=22 floor and CI coverage for Node 22. No global runtime change.
- Ruling: Use direct on-chain token spot prices only; reject missing prices instead of applying an underlying-share multiplier. This avoids double scaling. Live issuer/RPC preflight corrected Robinhood USDG to 6 decimals, covered by a regression test. Weekend stock trading stays paused conservatively.
- Ruling: The current documented Classic API exposes receiver/allowPartialFill/usePermit2 but no documented force-generic switch. Request safe documented flags and accept only decoded generic swap; reject optimized selectors. Route availability may be reduced. Exact approvals settle as separate cycles before obtaining fresh calldata; pending hashes block new signing. Two-confirmation receipt checks precede fresh valuation. Stale lock recovery serializes reapers; a crashed acquisition guard requires manual removal after process verification.
- Ruling: Unknown Fusion submission remains reserved until on-chain reconciliation/cancellation; no new order or blind write retry. Scan factory events independently to discover omitted partial fills. Escrow cancellation uses validated publicCancel only when eth_call confirms permission. Expired is never treated as refunded. Generic swap preview uses disableEstimate=true to avoid an approval/calldata deadlock, with local gas estimation and full calldata validation before signing (regression RED then GREEN). 0G private-tier glm-5.3 smoke test returned HTTP 200, 39 tokens, with no fallback and no key output.
- Ruling: Pulled exact response-proof verification from Task 8 into activation so owner credentials are never sent before an attested status/challenge check. Runtime uses an owner-supplied public agent ID because sealed exports AGENT_SEAL but not a guaranteed token ID environment variable. It checks the ID-to-seal mapping before loading state. Simulation with no observed balances seeds a clearly labeled paper balance of 1000 funding-chain stablecoins. Mint preflight requires existing sandbox credit; arbitrary deposits are never automatic.
- Ruling: Use one coherent timestamp for synthetic snapshots after the real CLI smoke test exposed clock drift; CLI simulation now fails on any worker error. Runtime/CLI integration fixtures replace a separate integration.test.ts. Cost if wrong: fixture gaps require funded deployment evidence, which remains explicitly unverified.
- Final: fixed clean-install lock mismatch — npm 10 ci --dry-run failed on missing nested utf-8-validate 5.0.10, regenerated only missing lock entries with npm 10, same dry-run now passes; remote clean-install CI rerun requested.
- Final: review — gpt-6-astra independent read-only whole branch review; 8 Important findings accepted, no Critical or Minor findings established.
- Final: Ruling: Use the first signature-verified, on-chain-approved framework measurement as the local image pin, with an exact current iData set and expected submitter — current AgenticID exposes an image allowlist but no per-agent image getter; reject later measurement changes. Cost if wrong: initial trust remains in the registry-approved image set rather than a separately audited build digest.
- Final: Ruling: Funded deployment, transfer/clone, live settlement and real proof capture remain explicitly unverified — the completed probes used inference credit and read-only market APIs, not wallet signing. Cost if wrong: deployment-specific failures may still appear in the funded experiment.
- Final: Ruling: Retain deterministic factory-derived escrow addresses and official creation events with the existing implementation substring check — the reviewer found no practical identity bypass requiring a separate full-bytecode fingerprint. Cost if wrong: a compromised or unexpectedly changed official factory/implementation is outside the tested assumptions.
- Final: Ruling: The independent clean-install lock defect is resolved by the parent — npm10 RED-to-GREEN and clean Node22 Windows/Linux CI passed at 3d46809. Cost if wrong: another package-manager/platform combination could still need validation.
- Final: fixed simulation recovery writes — never resumes live pending execution when configured for simulation RED→GREEN; full suite 143 passed, 1 Linux-only test skipped on Windows.
- Final: fixed crash-before-broadcast deadlock — recovers a crash before broadcast by resending only persisted signed bytes RED→GREEN; full suite 143 passed, 1 skipped.
- Final: fixed interrupted dependency install — retries an interrupted dependency install and marks only a successful matching lock RED→GREEN; full suite 143 passed, 1 skipped.
- Final: fixed old escrow recovery scan — advances and persists bounded terminal scans after an outage over 20000 blocks RED→GREEN; full suite 143 passed, 1 skipped.
- Final: fixed incomplete proof binding — rejects empty/subset/duplicate/stale framework/wrong submitter bindings RED→GREEN, plus adapter test for signature/registry/current data and first-use pin; full suite 143 passed, 1 skipped.
- Final: fixed stop/configure and shutdown races — completed stop invalidates earlier configuration and active cycle drain RED→GREEN; full suite 143 passed, 1 skipped.
- Final: fixed lost infrastructure settings — preserves validated runtime infrastructure without credentials RED→GREEN; full suite 143 passed, 1 skipped.
- Final: fixed multiline private environment values — preserves selected multiline dotenv values and rejects unterminated prompt RED→GREEN; full suite 143 passed, 1 skipped.
